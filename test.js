const galleryImages = []; let currentLightboxIndex = 0; const mainImgObj = {src: ''}; function openLightbox() { let lightbox = document.getElementById('nava-lightbox'); if (!lightbox) { lightbox = document.createElement('div'); lightbox.id = 'nava-lightbox'; lightbox.innerHTML = ''; lightbox.querySelector('div').onclick = closeLightbox; document.body.appendChild(lightbox); } const currentSrc = mainImgObj.src; currentLightboxIndex = galleryImages.findIndex(src => currentSrc.includes(src.split('?')[0])); if(currentLightboxIndex === -1) currentLightboxIndex = 0; const lightboxDiv = lightbox.querySelector('div'); lightbox.style.display = 'block'; void lightboxDiv.offsetWidth; lightboxDiv.style.opacity = '1'; updateLightboxImage(); document.body.style.overflow = 'hidden'; } function updateLightboxImage(){} function closeLightbox(){}
