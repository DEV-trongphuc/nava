Trong thư mục này là các BWT của Sapo đang chạy trên trang sapo của nava (Sapo BWT đang chạy) để xem cách hoạt động lấy content, checkout, lấy sản phẩn so sánh, collection, production, tồn kho,.... 
Chỉ đọc các file này và tạo ra các file mới để phù hợp với UI mới đang dùng và truy xuất các thông tin, product phù hợp để sử dụng render, so sánh hoặc AI tìm.

---
